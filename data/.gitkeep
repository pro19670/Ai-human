# 이 파일은 빈 디렉토리를 Git에 포함시키기 위한 것입니다.
# 실제 운영 시에는 users.jsonl, sessions.jsonl, leads.jsonl 파일이 자동 생성됩니다.

# 데이터 파일 형식 예시:
# users.jsonl - 사용자 정보 (한 줄당 하나의 JSON 객체)
# sessions.jsonl - 세션 로그 (선택적)
# leads.jsonl - 상담 신청 데이터